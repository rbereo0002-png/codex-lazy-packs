# Codex 懶人包 #02.5：連接 GitHub 與 Obsidian 第二大腦

> 版本：v0.5（Codex Desktop 版）
> 更新日期：2026-09-22
> 對應影片：Codex基本功 EP03（建議主題）

> 本篇是「Codex 版整合實作篇」。GitHub 與 Obsidian 的基礎觀念在 Claude 基本功系列已經講過，本篇只聚焦 Codex 要完成的兩件事：
>
> 1. 用 GitHub CLI 讓 Codex 可以 `commit` / `push` 到 GitHub。
> 2. 找到使用者的 Obsidian vault，安裝 MCPVault，讓 Codex 可以讀寫第二大腦。

---

## 公開網址

本版為個人實測修訂副本，原始懶人包與歷史來自 [mathruffian-dot/codex-lazy-packs](https://github.com/mathruffian-dot/codex-lazy-packs)。

- Codex 懶人包總入口：[https://github.com/rbereo0002-png/codex-lazy-packs](https://github.com/rbereo0002-png/codex-lazy-packs)
- 本篇公開網址：[https://github.com/rbereo0002-png/codex-lazy-packs/blob/master/02.5-%E9%80%A3%E6%8E%A5GitHub%E8%88%87Obsidian.md](https://github.com/rbereo0002-png/codex-lazy-packs/blob/master/02.5-%E9%80%A3%E6%8E%A5GitHub%E8%88%87Obsidian.md)

> [!tip]
> 影片說明欄建議放「本篇公開網址」。如果觀眾想看全部 Codex 懶人包，再引導他們回到總入口。

---

## 本篇和 Claude 基本功的關係

Claude 基本功裡，GitHub 與 Obsidian 拆成多集講：

| 主題 | Claude 版重點 | Codex 本篇怎麼處理 |
|------|---------------|--------------------|
| GitHub | Git、GitHub CLI、登入、Pages、發布 | 只做 Codex 需要的 `gh auth`、`commit`、`push` 驗證 |
| Obsidian | 安裝 Obsidian、建立 vault、同步方案、MCP | 不重講 Obsidian 教學，只找 vault 並設定 Codex MCP |

> [!tip]
> 如果你完全不知道 GitHub 是什麼，或還沒建立 Obsidian 筆記本，先回去看 Claude 基本功對應集數。本篇假設你大致知道 GitHub / Obsidian 是什麼，只需要把它們接進 Codex。

---

## 這個懶人包會幫你做什麼？

完成後，Codex 可以：

- 檢查 Git / GitHub CLI 是否已安裝
- 用網頁端登入 GitHub CLI
- 設定 Git 使用者資訊
- 建立測試 repo，驗證 Codex 可以 commit / push
- 找到既有 Obsidian vault
- 安裝 `@bitbonsai/mcpvault`
- 寫入 Codex MCP 設定
- 重啟後驗證 Codex 可以讀寫 Obsidian 筆記

---

## 先備條件

- [ ] 已安裝 Codex Desktop app
- [ ] 已有 GitHub 帳號
- [ ] 已有 Obsidian vault，或知道自己的筆記本在哪裡
- [ ] 電腦有網路
- [ ] Windows 使用者建議先完成 [[Codex 懶人包/01.5-Codex必裝Skills與Plugins]] 的基礎工具快篩

> [!warning]
> 本篇不是完整 Obsidian 入門教學。若使用者還沒安裝 Obsidian 或還沒有 vault，請先讓他照 Claude 基本功 Obsidian 集數建立筆記本，再回來跑本篇。

> [!important]
> GitHub 跟 GitHub Copilot 是不同服務。本篇只需要 GitHub 免費帳號、Git、GitHub CLI，不需要 GitHub Copilot Pro / Pro+ 訂閱。近期 GitHub Copilot 個人方案改成 AI Credits 計費，影響的是 Copilot 這個 AI 寫程式服務，不影響 Codex 使用 GitHub CLI 進行 `commit` / `push` 的流程。

---

## 整體流程

```text
階段一：GitHub CLI
  1. 檢查 Git / gh
  2. 網頁端登入 gh
  3. 設定 Git 使用者
  4. 建立測試 repo，驗證 commit / push

階段二：Obsidian MCP
  1. 找到 Obsidian vault
  2. 檢查 Node.js / npm
  3. 安裝 mcpvault
  4. 寫入 ~/.codex/config.toml
  5. 重啟 Codex
  6. 測試讀寫筆記
```

---

# 階段一：設定 GitHub CLI

## 步驟一：檢查 Git 與 GitHub CLI

請 Codex 執行：

```powershell
git --version
gh --version
```

如果 Git 沒有安裝，Windows：

```powershell
winget install --id Git.Git --accept-source-agreements --accept-package-agreements
```

如果 GitHub CLI 沒有安裝，Windows：

```powershell
winget install --id GitHub.cli --accept-source-agreements --accept-package-agreements
```

安裝後，請完全重啟 Codex Desktop app，再重新檢查。

> [!important]
> GitHub 外掛程式能讓 Codex 讀 GitHub；但要讓 Codex 在本機 `commit` / `push`，仍然需要 Git 與 GitHub CLI。

---

## 步驟二：用網頁端登入 GitHub CLI

先檢查：

```powershell
gh auth status
```

如果尚未登入：

```powershell
gh auth login --web --git-protocol https
```

流程：

1. PowerShell 顯示一次性驗證碼。
2. 瀏覽器開啟 GitHub device login。
3. 若沒自動開啟，手動開：

```text
https://github.com/login/device
```

4. 輸入驗證碼並授權。
5. 回到 Codex，重新執行：

```powershell
gh auth status
```

成功時應看到：

```text
Logged in to github.com account <你的帳號>
Git operations protocol: https
Token scopes: ... repo ...
```

---

## 步驟三：設定 Git 使用者資訊

檢查：

```powershell
git config --global user.name
git config --global user.email
```

如果沒有設定，請詢問使用者姓名與 email：

```powershell
git config --global user.name "你的姓名"
git config --global user.email "你的email@example.com"
```

---

## 步驟四：建立測試 repo，驗證 Codex 可以 commit / push

建立測試資料夾：

```powershell
New-Item -ItemType Directory -Path "$env:USERPROFILE\Documents\codex-github-test" -Force
Set-Location "$env:USERPROFILE\Documents\codex-github-test"
```

建立測試檔：

```powershell
@"
# Codex GitHub 測試

如果這個檔案成功出現在 GitHub，代表 Codex 可以 commit 與 push。
"@ | Set-Content -Encoding UTF8 -Path ".\README.md"
```

初始化並提交：

```powershell
git init
git add README.md
git commit -m "建立 Codex GitHub 測試"
```

建立 GitHub repo 並 push：

```powershell
gh repo create codex-github-test --private --source=. --push
```

驗證：

```powershell
gh repo view codex-github-test --web
```

> [!warning]
> 若瀏覽器開啟需要授權，請讓使用者手動確認。若不想開瀏覽器，可改用 `gh repo view codex-github-test` 查看。

---

## 步驟五：測試 repo 要保留還是刪除

測試成功後，請問使用者：

```text
GitHub 測試成功。這個 codex-github-test repo 要保留，還是刪除？
```

若要刪除 GitHub repo：

```powershell
gh repo delete codex-github-test --yes
```

若要刪除本機測試資料夾：

```powershell
$target = Join-Path $env:USERPROFILE "Documents\codex-github-test"
if ((Test-Path -LiteralPath $target) -and ((Resolve-Path $target).Path -eq (Join-Path $env:USERPROFILE "Documents\codex-github-test"))) {
    Remove-Item -LiteralPath $target -Recurse -Force
}
```

> [!important]
> 刪除 repo 或資料夾前一定要問使用者。

---

# 階段二：設定 Obsidian MCPVault

## 步驟六：找到使用者的 Obsidian vault

先問使用者：

```text
你的 Obsidian 筆記本放在哪裡？如果不知道，我可以幫你找。
```

常見位置：

| 同步方式 | 常見路徑 |
|----------|----------|
| OneDrive | `C:\Users\<你>\OneDrive\文件\<vault名稱>` |
| Google Drive | `G:\我的雲端硬碟\<vault名稱>` |
| Documents | `C:\Users\<你>\Documents\<vault名稱>` |
| Obsidian Sync | 本機任意資料夾，只要裡面有 `.obsidian` |

如果使用者不知道，Windows 可搜尋：

```powershell
$roots = @(
  "$env:USERPROFILE\OneDrive",
  "$env:USERPROFILE\Documents",
  "$env:USERPROFILE\Desktop",
  "G:\我的雲端硬碟",
  "G:\My Drive"
)

$roots |
  Where-Object { Test-Path $_ } |
  ForEach-Object {
    Get-ChildItem -Path $_ -Recurse -Directory -Force -ErrorAction SilentlyContinue |
      Where-Object { Test-Path (Join-Path $_.FullName ".obsidian") } |
      Select-Object FullName
  }
```

找到候選 vault 後，請使用者選主要 vault，不要自行猜。

確認條件：

- 資料夾存在
- 裡面有 `.obsidian`
- 使用者確認這是他的主要筆記本

把路徑記成：

```text
<VAULT_PATH>
```

---

## 步驟七：檢查 Node.js 與 npm

mcpvault 需要 Node.js / npm。

請執行：

```powershell
node --version
npm.cmd --version
```

如果沒有 Node.js，Windows：

```powershell
winget install --id OpenJS.NodeJS --accept-source-agreements --accept-package-agreements
```

安裝後重啟 Codex Desktop app，再確認：

```powershell
node --version
npm.cmd --version
```

---

## 步驟八：安裝 mcpvault

Windows PowerShell 建議用 `npm.cmd`，避免 PowerShell 執行原則擋到 `npm.ps1`。

```powershell
npm.cmd install -g @bitbonsai/mcpvault
```

確認安裝位置：

```powershell
where.exe mcpvault
```

常見位置：

```text
C:\Users\<你>\AppData\Roaming\npm\mcpvault.cmd
```

把這個完整路徑記成：

```text
<MCPVAULT_CMD>
```

---

## 步驟九：寫入 Codex MCP 設定

Codex 設定檔通常在：

```text
C:\Users\<你>\.codex\config.toml
```

請先備份：

```powershell
$config = "$env:USERPROFILE\.codex\config.toml"
$backup = "$config.bak-$(Get-Date -Format 'yyyyMMdd-HHmmss')"
Copy-Item -LiteralPath $config -Destination $backup -Force
```

加入或更新以下區塊：

```toml
[mcp_servers.obsidian]
command = "<MCPVAULT_CMD>"
args = ["<VAULT_PATH>"]
startup_timeout_sec = 20
tool_timeout_sec = 60
```

Windows 範例：

```toml
[mcp_servers.obsidian]
command = "C:\\Users\\mathr\\AppData\\Roaming\\npm\\mcpvault.cmd"
args = ["C:\\Users\\mathr\\OneDrive\\文件\\Secondbrain"]
startup_timeout_sec = 20
tool_timeout_sec = 60
```

> [!warning]
> Windows TOML 路徑要用雙反斜線 `\\`。
> `args` 裡的 vault 路徑也要用 `\\`。

上述跳脫規則適用於 TOML 雙引號字串；也可以使用單引號 literal string，例如 `command = 'C:\Users\<你>\AppData\Roaming\npm\mcpvault.cmd'`，此時保留單一反斜線。請替換為實際路徑，並先檢查是否已有 `[mcp_servers.obsidian]`，避免重複加入同名區塊。若已安裝 Codex CLI，可用 `codex mcp get obsidian` 確認設定能被讀取；這仍不等於讀寫測試成功。

---

## 步驟十：手動測試 mcpvault

在重啟 Codex 前，先測 mcpvault 本身能不能啟動。

```powershell
'{"jsonrpc":"2.0","id":1,"method":"tools/list"}' |
  & "<MCPVAULT_CMD>" "<VAULT_PATH>"
```

如果成功，應該會看到工具清單，包含類似：

```text
read_note
write_note
search_notes
list_directory
get_vault_stats
```

若這一步失敗，先不要重啟 Codex，先排查路徑。

這段單次管線僅作快速檢查。2026-09-22 實測中，直接把 `tools/call` 接到短命管線曾出現退出碼為 0、卻沒有建立測試筆記的情況。必須檢查實際 JSON-RPC 回應與讀回內容，不能只看退出碼。自行編寫 MCP 測試程式時，應完成 `initialize` → `notifications/initialized` → `tools/list`／`tools/call`，保持程序與 stdin 開啟，等待相符 request id 的回應後再關閉；也可直接依步驟十一，使用 Codex 載入的工具驗證。

---

## 步驟十一：重啟 Codex 並驗證 Obsidian MCP

請使用者：

1. 完全關閉 Codex Desktop app
2. 重新開啟
3. 回到任一專案
4. 請 Codex 檢查 Obsidian MCP 是否可用

測試方式：

```text
請列出我的 Obsidian vault 根目錄資料夾。
```

再測寫入：

```text
請在我的 Obsidian 筆記本建立 `Codex 測試/GitHub-Obsidian-連線測試.md`，內容寫入今天日期、目前專案名稱，以及「Codex 已成功連接 Obsidian MCP」。
```

最後讀回：

```text
請讀回剛剛建立的 Obsidian 測試筆記。
```

---

## 完成回報格式

請 Codex 完成後用這個格式回報：

```md
## GitHub / Obsidian 連接完成

### GitHub

- Git：已安裝 / 已補裝
- GitHub CLI：已安裝 / 已補裝
- gh 登入：成功 / 失敗
- Git 使用者資訊：已設定 / 待設定
- commit / push 測試：成功 / 未執行
- 測試 repo：保留 / 已刪除 / 未建立

### Obsidian

- Vault 路徑：<VAULT_PATH>
- mcpvault：已安裝 / 未安裝
- Codex MCP 設定：已寫入 / 未寫入
- config.toml 備份：<備份路徑>
- 重啟 Codex：已完成 / 待使用者完成
- 讀取測試：成功 / 失敗
- 寫入測試：成功 / 失敗

### 下一步

1. 實際專案要發布時，可直接請 Codex commit / push。
2. 工作收尾時，把踩坑與進度寫回 Obsidian。
3. 若要全域自動化，後續再設定開工 / 收工 skills。
```

---

## 踩坑筆記

| 狀況 | 原因 | 解法 |
|------|------|------|
| `git` 找不到 | Git 未安裝或 Codex 尚未重啟 | 安裝 Git 後重啟 Codex |
| `gh auth status` 顯示未登入 | GitHub CLI 尚未授權 | `gh auth login --web --git-protocol https` |
| `gh auth login --web` 在 Codex 裡等到逾時 | GitHub 登入需要使用者到瀏覽器完成授權，Codex 無法代替輸入帳密 | 開一個可互動 PowerShell 視窗執行 `gh auth login --web --clipboard --git-protocol https --hostname github.com`，使用者完成瀏覽器授權後再回來驗證 |
| `gh auth status` 出現 `failed to read configuration` 或 `Access is denied` | Codex 沙盒讀不到 `AppData\Roaming\GitHub CLI\config.yml` | 允許 Codex 讀取 GitHub CLI 設定後重跑；這不一定代表 GitHub 尚未登入 |
| `gh --version` Access denied | Codex 讀 GitHub CLI 設定被擋 | 允許 Codex 讀取後重跑 |
| GitHub 外掛能讀但不能 push | 外掛不等於本機 Git 權限 | 檢查 `gh auth status` |
| Git 全域姓名 / email 是空的 | 只安裝 Git 不會自動建立 commit 作者資訊 | 用 `git config --global user.name` 與 `git config --global user.email` 補上；不想公開信箱可用 GitHub no-reply email |
| `npm.ps1` 被擋 | PowerShell 執行原則 | 改用 `npm.cmd` |
| `npm.cmd install -g @bitbonsai/mcpvault` 出現 `EPERM` 或無法寫入 npm cache | Codex 沙盒沒有權限寫入 `AppData\Local\npm-cache` 或全域 npm 目錄 | 改用已授權 / 提權模式重跑同一個 npm 安裝指令 |
| `where mcpvault` 找不到 | npm 全域路徑不在 PATH | 到 `AppData\Roaming\npm` 找完整路徑 |
| mcpvault 已安裝但 `where.exe mcpvault` 仍找不到 | 目前的 Codex / PowerShell 程序還沒刷新 PATH | 用 `npm.cmd prefix -g` 找全域路徑，直接使用 `C:\Users\<你>\AppData\Roaming\npm\mcpvault.cmd` 測試 |
| TOML 路徑失敗 | Windows 反斜線沒跳脫 | 使用 `C:\\Users\\...` |
| TOML 或終端機顯示 `文件` 變成亂碼 | 終端機編碼顯示問題，不一定代表檔案路徑真的壞掉 | 以實際 mcpvault 測試結果為準；工具清單只驗證啟動；還須列出目標 Vault 目錄並讀回含中文的測試筆記 |
| 改完 config 後工具沒出現 | Codex 尚未重啟 | 完全關閉再開 Codex |
| 找不到 vault | 路徑錯或不是 Obsidian vault | 搜尋 `.obsidian` 並請使用者確認 |
| 找到 OneDrive 與 Documents 兩個相似 vault | 可能是同步資料夾與本機副本，內容看起來一樣 | 優先選使用者實際同步中的 vault，例如 `OneDrive\文件\Secondbrain`，並確認裡面有 `.obsidian` |
| 下載到本機的 Markdown 看起來是亂碼 | 本機檔案或讀取工具的編碼與 UTF-8 不一致 | 回到 GitHub repo 原始檔確認內容，不要直接依亂碼版本修改 |

---

## 2026-09-22 Windows 安裝實測補充

以下整理本次實際安裝紀錄。路徑以通用範例表示；不要照抄其他人的帳號、磁碟代號、憑證或已過期的裝置授權碼。

### 1. winget 下載失敗，改用瀏覽器安裝後仍無法登入

本次先遇到 `InternetOpenUrl() failed`、`0x80072f0d`；PowerShell 下載也回報 `Authentication failed`。使用者透過瀏覽器完成 GitHub CLI 安裝後，`gh auth login` 仍出現：

```text
tls: failed to verify certificate: x509: certificate signed by unknown authority
```

這表示「程式已安裝」與「CLI 的 HTTPS 信任鏈正常」是兩個檢查項目。瀏覽器可以下載，不代表 gh 的 TLS 問題已修好。

先執行不帶登入憑證的連線檢查：

```powershell
Get-Date
curl.exe -I https://github.com --max-time 15
netsh winhttp show proxy
```

接著由熟悉憑證管理的人員檢查伺服器憑證的簽發者、有效期與完整鏈，判斷是 Windows 根憑證缺漏，還是機關代理／HTTPS 檢查設備造成。不能把所有 TLS 錯誤都當成沙盒權限問題。

本次在沙盒外也重現「不受信任的根憑證」，最後確認 Windows 缺少 `USERTrust ECC Certification Authority`；Windows Update 根憑證下載亦失敗。經核對本機 Node.js 所附根憑證與憑證機構官方資料、取得使用者明確同意後，只將該張憑證加入 `Cert:\CurrentUser\Root`，再測 GitHub HTTPS 與 gh 登入，才恢復正常。

這是本次診斷結果，不是每台電腦都要匯入的固定安裝步驟。新增受信任根憑證會持久改變 HTTPS 信任範圍，須先核對來源、指紋、必要性及機關規範，再取得明確授權。本次首次匯入也因自動核准審查要求明確同意而暫停。不要使用 `curl -k`、`GIT_SSL_NO_VERIFY` 或關閉 TLS 驗證來繞過錯誤。

修復後重新執行登入，取得當次有效的裝置碼：

```powershell
gh auth login --hostname github.com --git-protocol https --web
gh auth status
gh api user --jq .login
```

若連裝置碼都未產生就出現 TLS 錯誤，應先修復連線，不能要求使用者一直重做瀏覽器授權。下載安裝檔可從 [GitHub CLI 官方下載頁](https://cli.github.com/) 進入；不要保存帶有到期時間的 release-assets 轉址作為教學連結。

### 2. gh 已安裝，但目前的 Codex 程序找不到指令

先確認實際安裝位置，不要立即重裝：

```powershell
Get-Command gh -ErrorAction SilentlyContinue
Test-Path -LiteralPath 'C:\Program Files\GitHub CLI\gh.exe'
```

若第二項為 `True`，可用完整路徑測試：

```powershell
& 'C:\Program Files\GitHub CLI\gh.exe' --version
& 'C:\Program Files\GitHub CLI\gh.exe' auth status
```

之後完全關閉並重新開啟 Codex／PowerShell，再驗證 PATH。後續範例若仍無法直接呼叫 `gh`，同樣以已確認的完整路徑代替。

### 3. Git 提交身分、GitHub 登入與 push 要分別驗證

本次 Git 已安裝，但全域 `user.name`／`user.email` 是空的。向使用者取得提交身分後再設定；若已有設定，不應任意覆寫。Git 作者資訊不是 GitHub 登入憑證，能登入也不能取代 commit 作者設定。

建立測試 repo 前，先確認本機目錄與遠端同名 repo 是否已存在；如果存在，檢查內容後沿用或改名，避免用 `-Force` 加上寫入 README 的步驟覆蓋既有檔案。本次先確認不存在，再建立私有測試 repo，並執行 `gh auth setup-git` 設定 HTTPS credential helper。

push 後，在該測試 repo 目錄內比對本機與遠端提交：

```powershell
git rev-parse HEAD
gh repo view --json nameWithOwner,isPrivate,url
gh api 'repos/{owner}/{repo}/commits/HEAD' --jq .sha
git status --short
```

確認兩個 SHA 相同、遠端是預期的私有 repo，且工作目錄狀態符合預期，才回報 commit／push 成功。本次已通過比對，使用者選擇保留測試 repo 與本機資料夾。

### 4. 舊的 secondbrain 資料夾存在，不代表它是目前的主要 Vault

本次原先記錄的雲端硬碟 `secondbrain` 仍存在，也有筆記子資料夾，但未找到 `.obsidian`；使用者最後實際開啟的是另一個磁碟上的「第二大腦」。因此不能直接沿用舊紀錄，也不能把 Google Drive 磁碟代號固定寫成 G: 或 F:。

先以 Windows 搜尋開啟 Obsidian。若已進入筆記畫面，可從左下角筆記庫名稱進入管理筆記庫，或按 `Ctrl+P` 搜尋 `vault`／`筆記庫`，尋找「開啟其他筆記庫」。歡迎畫面則選「開啟資料夾為筆記庫」。選單名稱可能因版本及語言不同。

可讀取 Obsidian 已登記的路徑以縮小搜尋範圍：

```powershell
$obsidianRegistry = Join-Path $env:APPDATA 'obsidian\obsidian.json'
if (Test-Path -LiteralPath $obsidianRegistry) {
    $obsidianState = Get-Content -LiteralPath $obsidianRegistry -Raw | ConvertFrom-Json
    $obsidianState.vaults.PSObject.Properties.Value | Select-Object path,open
}
```

這份檔案可能尚未存在或留有舊紀錄。仍須核對使用者實際開啟的資料夾、筆記內容及主要 Vault 選擇。缺少 `.obsidian` 只表示尚未通過本篇辨識步驟，不代表筆記已遺失，也不應直接建立空資料夾偽裝成完成。找到不同 Vault 時，先確認目標再寫入 MCP 設定。

### 5. MCP 已安裝、已設定、可讀寫，是三種不同狀態

本次檢查時 MCPVault 0.16.0 已存在，因此先驗證現有安裝，不必重複安裝。寫入 Codex 設定前備份檔案，使用實際的 `mcpvault.cmd` 與 Vault 完整路徑。中文路徑需檢查真實讀寫結果；若以 PowerShell 管線傳中文 JSON，可先設定：

```powershell
$OutputEncoding = [System.Text.UTF8Encoding]::new($false)
```

手動測試須取得實際工具回應，詳見步驟十。完成後再重啟 Codex，透過對話中的 Obsidian MCP 工具列目錄、建立唯一名稱的測試筆記並讀回。若同名測試筆記已存在，先讀取或改用新檔名，不要直接覆寫。

驗收時分開回報：套件存在、設定能解析、手動 MCP 測試成功、Codex 重啟後工具可用、寫入及讀回成功。本次後續已透過 Codex 的 Obsidian MCP 更新測試筆記，GitHub 與 Obsidian 驗證均完成。

Codex 的 MCP 設定與檢查方式參考 [OpenAI 官方 MCP 文件](https://learn.chatgpt.com/docs/extend/mcp?surface=cli)。

---

## 本篇不做的事

- 不完整重講 GitHub 是什麼
- 不完整重講 Obsidian 怎麼用
- 不強迫新建 Obsidian vault
- 不強迫使用 Google Drive 或 OneDrive
- 不把 GitHub token 寫進 `AGENTS.md`
- 不在未確認前刪除測試 repo 或測試資料夾

---

## 相關懶人包

- [[Codex 懶人包/01.5-Codex必裝Skills與Plugins]]
- [[Codex 懶人包/02-連接-GitHub]]
- [[Codex 懶人包/03-建立第二大腦-Obsidian]]
- [[Codex 懶人包/09-用chezmoi同步Codex設定]]

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|----------|
| 2026-09-22 | v0.5 | 補充 Windows 實測：winget／gh TLS 根憑證缺漏、安裝與登入分開驗證、gh 完整路徑、Vault 路徑變更、TOML 單引號路徑、MCP 短命管線誤判與實際讀寫驗收、遠端提交 SHA 比對 |
| 2026-04-28 | v0.4 | 補充 Codex Desktop 實測踩坑：GitHub 登入逾時、GitHub CLI 設定權限、npm 全域安裝 EPERM、mcpvault PATH、vault 候選與編碼顯示問題 |
| 2026-04-28 | v0.3 | 補上 Codex 懶人包總入口與本篇公開網址 |
| 2026-04-28 | v0.2 | 補充 GitHub 與 GitHub Copilot 差異，確認不需 Copilot 訂閱 |
| 2026-04-28 | v0.1 | 初版，整合 Codex GitHub CLI commit / push 與 Obsidian MCPVault 連接 |
